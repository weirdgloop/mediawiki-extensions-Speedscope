name: Lint code
on:
  push:
jobs:
  lint:
    uses: weirdgloop/ci-workflows/.github/workflows/extension-lint.yml@main
    with:
      php-version: '8.2'
      php-extensions: 'zlib, excimer'
      node-version: '24'
